node_modules
.env
.env.local
.DS_Store
npm-debug.log*

# Local persistent-data dir for testing the admin (DATA_DIR)
.localdata/

# Uploaded product photos live on the data disk (DATA_DIR), never in the repo
product-images/
